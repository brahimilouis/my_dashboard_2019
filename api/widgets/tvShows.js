//const series = require("tv-shows");


function tvShows(req, res)
{
   // if (
  //      !(req.session.logged && req.session.logged === true)
  //  ) {
  //      res.send("false")
   // } else {
  //      series.findShows(req.body.series, function(list){
  //          res.send({
 //               name: list[0]["show"]['name'],
 //               type: list[0]["show"]['type'],
     //          language: list[0]["show"]['language'],
   //             premiered: list[0]["show"]["premiered"],
    //            url: list[0]["show"]["url"],
   //             img: list[0]["show"]["image"]["medium"]
 //           })
//        });
//    }
}

module.exports = tvShows
